import numpy as np
import tensorflow as tf

#a = np.array(range(6), int)
#a.sort()

#print(help(a.sort))

#a = np.arange(6, dtype=float).reshape(3,2)

#a = np.array([[6, 2, 5],[1, 7, 0]], int)

#np.clip(a, 0, 5, out=a)

#a = a.tolist()

#print(a)

lam = lambda x,y : (x + y)

print(lam(4, 6))

